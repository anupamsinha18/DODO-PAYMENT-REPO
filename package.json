{
  "name": "dodo-checkout-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Tiny Embeddable Checkout - Take-home assignment for Dodo Payments",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n \"checkout,demo\" -c \"cyan,magenta\" \"npm run dev --workspace=@dodo/checkout\" \"npm run dev --workspace=@dodo/demo\"",
    "build": "npm run build --workspace=@dodo/sdk && npm run build --workspace=@dodo/checkout && npm run build --workspace=@dodo/demo",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  }
}
