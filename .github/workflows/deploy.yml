name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build-and-deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"

      - name: Install dependencies
        run: npm install

      - name: Run automated test suites
        run: npm test

      - name: Build SDK
        run: npm run build --workspace=@dodo/sdk

      - name: Build Checkout App (GitHub Pages base)
        env:
          GITHUB_PAGES: "true"
        run: npm run build --workspace=@dodo/checkout

      - name: Build Demo Store (GitHub Pages base)
        env:
          GITHUB_PAGES: "true"
        run: npm run build --workspace=@dodo/demo

      - name: Combine build artifacts
        run: |
          mkdir -p apps/demo/dist/checkout
          cp -r apps/checkout/dist/* apps/demo/dist/checkout/

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: "apps/demo/dist"

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
